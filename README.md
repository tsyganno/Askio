***

# 🧠 Askio — RAG-сервис для "Умного FAQ"

## 📋 Описание проекта

**Askio** — это мини-сервис в стиле Retrieval-Augmented Generation (RAG), который позволяет загружать документы компании и получать интеллектуальные ответы на вопросы пользователей на основе этих документов.
Сервис реализован на **FastAPI**, использует **ChromaDB** для поиска релевантных фрагментов, **LLaMA** для генерации ответов, а также **Redis** и **PostgreSQL** для кэширования и хранения истории запросов.

***

## ⚙️ Основные технологии

* **FastAPI** — REST API и веб-интерфейс
* **PostgreSQL** — хранение истории запросов и документов
* **Redis** — кэширование ответов (TTL 1 час)
* **ChromaDB** — векторная база данных для поиска по контенту
* **LLaMA (llama.cpp)** — локальная LLM для генерации ответов
* **SQLAlchemy (async)** — ORM для работы с БД
* **Jinja2** — шаблоны для HTML-интерфейса
* **Docker Compose** — запуск всего проекта одной командой

***

## 🧩 Архитектура

```
app/
├── api/
│   └── endpoints.py        # FastAPI endpoints
├── core/
│   ├── config.py           # Настройки проекта (.env)
│   ├── logger.py           # Конфигурация логирования
├── database/
│   ├── models.py           # ORM-модели SQLAlchemy
│   ├── crud.py             # CRUD-функции (save_query, save_document)
│   ├── schema_models.py    # Pydantic схемы для валидации входных и выходных данных API
│   └── session.py          # Подключение к PostgreSQL
├── services/
│   ├── rag.py              # Основная логика RAG (Chroma + LLaMA)
│   ├── cache.py            # Работа с Redis
│   └── other_functions.py  # Разбиение текста на чанки
├── templates/              # HTML-шаблоны (index.html)
└── main.py                 # Точка входа (uvicorn run)
```

***

## 🔄 Алгоритм работы

### 1️⃣ Загрузка документов

* Пользователь отправляет `POST /api/documents` с файлами (`pdf`).
* Текст извлекается и разбивается на **чанки** (по 1000 символов с перекрытием 100).
* Каждый чанк сохраняется в PostgreSQL.
* Все чанки индексируются в **ChromaDB** для последующего семантического поиска.

### 2️⃣ Обработка вопроса (`POST /api/ask`)

1. Пользователь задаёт вопрос.
2. Сервис проверяет **Redis** — есть ли готовый ответ в кэше.

   * Если есть → возвращает мгновенно.
3. Если нет, выполняется **поиск релевантных чанков** в ChromaDB.
4. LLaMA оценивает релевантность фрагментов и выбирает наиболее подходящие.
5. На основе найденных фрагментов LLaMA генерирует ответ.
6. Ответ, токены и источники сохраняются в PostgreSQL и Redis (TTL = 1 час).

### 3️⃣ Интерфейс

* `/` — простая HTML-страница для отправки вопросов и просмотра ответов.
* `/api/health` — healthcheck endpoint.

***

## 🧠 Пример пайплайна

```
[Документы PDF/TXT/MD] → [Чанкование] → [ChromaDB]
                                 ↓
                            [Вопрос]
                                 ↓
                    [Поиск релевантных чанков]
                                 ↓
                      [LLaMA генерирует ответ]
                                 ↓
                 [Ответ + источники + кэш Redis]
```

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone https://github.com/username/askio.git
cd askio
```

### 2. Создать `.env`

```env
APP_HOST=0.0.0.0
APP_PORT=8000
DATABASE_URL=postgresql+asyncpg://us:test@localhost:5432/askio
REDIS_URL=redis://localhost:6379
REDIS_CACHE_TTL=3600
MODEL_PATH=llama/Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf
```
### 3. 📥 Скачать модель LLaMA 3.1
Перед запуском убедись, что у тебя есть директория llama в корне проекта и внутри неё лежит модель:
```bash
mkdir llama
cd llama
```
Затем скачай модель LLaMA 3.1 (Q4_K_M):
```bash
wget https://huggingface.co/bartowski/Meta-Llama-3.1-8B-Instruct-GGUF/resolve/main/Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf

```
💡 Если у тебя нет wget, можно скачать вручную через браузер по ссылке:
https://huggingface.co/bartowski/Meta-Llama-3.1-8B-Instruct-GGUF/resolve/main/Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf

После скачивания структура должна выглядеть так:
```
askio/
├── alembic/
├── app/
├── files/
├── llama/
│   └── Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf
├── screenshots/
├── tests/
├── .env
├── alembic.ini
├── docker-compose.yml
├── Dockerfile
├── README.md
├── requirements.txt
└── requirements-test.txt

```

### 4. Запустить через Docker Compose

```bash
docker compose up --build
```

### 5. Проверить

* API: [http://localhost:8000/api/health](http://localhost:8000/api/health)
* Веб-интерфейс: [http://localhost:8000](http://localhost:8000)

***

## 🧪 Тестирование

### Unit-тесты (pytest)

```bash
# Основная команда
docker exec -it askio_api python -m pytest tests/ -v
```

Тестовое покрытие Unit-тесты:

* ✅ test_ask_no_relevant_documents - работа RAGService при отсутствии релевантных документов

* ✅ test_ask_uses_cache - проверка кэширования в Redis

* ✅ test_ask_endpoint_success - тест структуры ответа API endpoint

* ✅ test_upload_documents_txt_success - загрузка TXT документов через /api/documents

* ✅ test_upload_multiple_documents - загрузка нескольких документов

* ✅ test_upload_documents_unsupported_format - обработка неподдерживаемых форматов

Интеграционные тесты:

* ✅ test_health_endpoint - проверка health check endpoint

* ✅ test_ask_endpoint_structure - валидация запросов к API

* ✅ test_ask_endpoint_server_error - обработка ошибок сервера


Покрывают:

* работу `RAGService.ask` при отсутствии релевантных документов
* кэширование в Redis
* загрузку документов `/api/documents`

### Интеграционный тест

* Проверяет `/api/ask` с реальным вопросом и ответом от модели.

***

## 📊 Преимущества реализации

* ⚡ Полностью асинхронный стек (FastAPI + async SQLAlchemy)
* 🧠 Использует локальную LLaMA — не требует API-ключей OpenAI
* 🧩 Автоматическая индексация документов в ChromaDB
* 🧺 Redis-кэширование часто задаваемых вопросов
* 🧾 Логирование и история запросов для аналитики
* 🧱 Удобный Docker Compose для быстрого запуска

***

## 👨‍💻 Автор

**Цыганов Дмитрий**
Python Backend Developer
[GitHub](https://github.com/tsyganno) • [Telegram](https://t.me/dmitriy_tsyganov)

***
